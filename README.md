# Next-Word-Prediction-Web-App-LSTM-Streamlit-
An interactive web application built with Streamlit that predicts the next possible word in a user-entered sentence using a pre-trained LSTM (Long Short-Term Memory) neural network.
